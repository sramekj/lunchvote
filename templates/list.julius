$(function(){
    $(".votebutton").click(function(event){
        $.ajax({
            url: "vote/"+event.target.id,
            success: function() {
                $("#" + event.target.id).hide();
             },
            type: "POST"
        });
    })
});
