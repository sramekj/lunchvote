<div .jumbotron .text-center>
    <h1>Pool results
<ul>
<div class="table-responsive">
    <table .table>
        <thead>
            <tr>
                <th> Restaurant
                <th> Votes
        <tbody class="pool-results">
            $forall voteresult <- results
                <tr>
                    <td>#{name voteresult} 
                    <td>#{votes voteresult}
